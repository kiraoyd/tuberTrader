{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "dev": "vite-node -w src/index.ts",
    "build": "vite build --emptyOutDir -c vite.config.ts -m development",
    "startProd": "http-server -p 8080 build/",
    "lint": "eslint . --ext .ts",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest --passWithNoTests",
    "typeorm": "pnpm typeorm",
    "buildJS": "pnpm tsc-esm -p tsconfig.migration.json",
    "typeorm:drop": "pnpm buildJS && typeorm schema:drop -d buildTypeorm/db/datasources/dev_datasource.js",
    "typeorm:sync": "pnpm buildJS && typeorm schema:sync -d buildTypeorm/db/datasources/dev_datasource.js",
    "typeorm:updateDatasource": "pnpm buildJS && typeorm-codebase-sync addReferences --dataSource ./src/db/datasources/dev_datasource.ts --migrations ./src/db/migrations --entities ./src/db/models --subscribers ./src/db/subscribers",
    "migration:run": "pnpm buildJS && typeorm migration:run -d buildTypeorm/db/datasources/dev_datasource.js",
    "migration:create": "pnpm buildJS && typeorm migration:create",
    "migration:generate": "pnpm buildJS && typeorm migration:generate -d buildTypeorm/db/datasources/dev_datasource.js",
    "migration:revert": "pnpm buildJS && typeorm migration:revert -d buildTypeorm/db/datasources/dev_datasource.js",
    "seed": "vite-node src/index.ts -- -seed",
    "generateDocs": "pnpm jsdoc -r -d docs/ -c jsdoc.json src/",
    "gitSync": "pnpm typeorm:drop && pnpm migration:run && pnpm seed"
  },
  "scriptsComments": {
    "dev": "Starts the development server using vite-plugin-node for rapid iteration.",
    "build": "Builds the app for production.",
    "startProd": "Runs the built app in production mode. Note this requires globally-installed npm package http-server (pnpm i -g http-server).",
    "lint": "Lints the project.",
    "test": "Runs the test suite once.",
    "test:watch": "Runs the test suite on changes.",
    "typeorm": "Necessary for updateDatasource to function properly, as it expects a discrete script named typeorm",
    "buildJS": "Builds standard JS files via tsc-esm, which we use for typeorm CLI and migrations",
    "typeorm:drop": "Resets typeorm's internal idea of your database's state as well as dropping all tables in the database if they exist.  Use with caution!",
    "typeorm:sync": "Syncs database to current Models state, like synchronize: true in our datasources, but on-demand",
    "typeorm:updateDatasource": "Updates the typeorm datasource file with the latest references to your models, migrations, etc. Run after migration:generate or migration:create",
    "migration:run": "Runs all pending migrations, use after typeorm:updateDatasource",
    "migration:create": "Creates new migration for manual entry, used like > pnpm migration:create ./src/db/migrations/MyNewMigrationName.  Run typeorm:updateDatasource after this to update the datasource file.",
    "migrations:generate": "Auto-generates a new migration based on any entity changes you made to your JS files, used like > pnpm migration:generate ./src/db/migrations/IpHistoryAddMethod  Run typeorm:updateDatasource after this to update the datasource file.",
    "migration:revert": "Undoes the most recent migration, can be called multiple times in a row",
    "seed": "Starts Doggr in seed mode (Create default database entries), used like > pnpm seed",
    "generateDocs": "Auto generates a doc site from JSDoc comments in your code after building",
    "gitSync": "Run this immediately after 'git pull' to update your local database to match the new commit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/cors": "^8.2.1",
    "@fastify/jwt": "^6.7.1",
    "@fastify/middie": "^8.2.0",
    "@fastify/multipart": "^7.6.0",
    "@fastify/oauth2": "^7.1.0",
    "@fastify/static": "^6.10.1",
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-openid-connect": "^2.16.0",
    "fastify": "^4.17.0",
    "fastify-auth0-verify": "^1.1.1",
    "fastify-cors": "^6.1.0",
    "fastify-env": "^2.2.0",
    "fastify-oauth2": "^4.6.0",
    "fastify-plugin": "^4.5.0",
    "jsonwebtoken": "^9.0.0",
    "minio": "^7.1.0",
    "pg": "^8.10.0",
    "pino": "^8.14.1",
    "reflect-metadata": "^0.1.13",
    "typeorm": "^0.3.15"
  },
  "devDependencies": {
    "@digitak/tsc-esm": "^3.1.4",
    "@faker-js/faker": "^7.6.0",
    "@swc/cli": "^0.1.62",
    "@swc/core": "^1.3.56",
    "@types/cors": "^2.8.13",
    "@types/node": "^18.16.6",
    "@types/supertest": "^2.0.12",
    "@typescript-eslint/eslint-plugin": "^5.59.5",
    "@typescript-eslint/parser": "^5.59.5",
    "@vitest/coverage-c8": "^0.28.5",
    "@vitest/ui": "^0.28.5",
    "better-docs": "^2.7.2",
    "c8": "^7.13.0",
    "esbuild": "^0.17.18",
    "eslint": "^8.40.0",
    "fastify-cli": "^5.7.1",
    "jsdoc": "^4.0.2",
    "pino-pretty": "^9.4.0",
    "pnpm": "^7.32.3",
    "prop-types": "^15.8.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "rimraf": "^4.4.1",
    "supertest": "^6.3.3",
    "taffydb": "^2.7.3",
    "ts-node": "^10.9.1",
    "tsconfig-paths": "^4.2.0",
    "typeorm-codebase-sync": "^1.1.2",
    "typescript": "^4.9.5",
    "vite": "^4.3.5",
    "vite-node": "^0.28.5",
    "vite-plugin-dts": "^1.7.3",
    "vite-plugin-node": "^3.0.2",
    "vitest": "^0.28.5"
  }
}